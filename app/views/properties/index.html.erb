<h1>All Available Properties</h1>
<% @properties.each do |property| %>
  <h4><%= link_to property.address + " - " + property.price, property_path(property) %></h4>
  <p><%= image_tag  property.image_url,  height: '200', width: '200' %></p>
<% end %>
<p><%= link_to 'Back', :back %></p>
